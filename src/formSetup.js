const formElement = document.getElementById("weatherSearch");
const userSearch = document.getElementById("user-input");

const formComponents = {
    formElement,
    userSearch
}

export default formComponents;